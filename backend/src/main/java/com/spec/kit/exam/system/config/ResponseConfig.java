package com.spec.kit.exam.system.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for unified response handling
 */
@Configuration
public class ResponseConfig {
    // Configuration for unified response format
    // This class can contain additional configuration for response handling
    // if needed, but for now it's a placeholder as the Result class handles
    // the unified response format
}